# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ProfileManagementServiceTemplate' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ProfileManagementServiceTemplate
  pod 'Alamofire', '~> 4.0'

  target 'ProfileManagementServiceTemplateTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ProfileManagementServiceTemplateUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
